public class Main {
    public static void main(String[] args) {
        new initial();
    }
}


/* import javax.swing.*;
import java.awt.*;

public class TopPanelExample {
    public static void main(String[] args) {
        // Create a JFrame
        JFrame frame = new JFrame("Top Panel Example");
        frame.setSize(400, 300);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        // Create a JPanel for the top panel
        JPanel topPanel = new JPanel();
        topPanel.setBackground(Color.BLUE);
        topPanel.setPreferredSize(new Dimension(frame.getWidth(), 50)); // Set preferred height

        // Add components to the top panel if needed
        JLabel titleLabel = new JLabel("Top Panel");
        topPanel.add(titleLabel);

        // Add the top panel to the frame's content pane
        frame.getContentPane().add(topPanel, BorderLayout.NORTH);

        // Add other components to the frame if needed
        JPanel contentPanel = new JPanel();
        contentPanel.setBackground(Color.WHITE);
        frame.getContentPane().add(contentPanel, BorderLayout.CENTER);

        // Display the frame
        frame.setVisible(true);
    }
}
*/